"""Settings for staging server.
    Settings based off production, as this environment should match
    production as closely as possible. Additional settings can be
    added as needed to support debugging.
"""
from .production import *  # pylint: disable=wildcard-import, unused-wildcard-import
